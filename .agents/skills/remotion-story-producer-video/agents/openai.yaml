interface:
  display_name: "Remotion Story 视频制作"
  short_description: "冻结生产并派发独立线程，由后台 watcher 自动交付"
  default_prompt: "使用 $remotion-story-producer-video，把我提供的完整内容冻结并派发到独立线程，由后台 watcher 自动收口交付；主任务派发后立即结束。"
