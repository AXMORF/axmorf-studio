# Third-party notices

AXMORF Studio is licensed under Apache-2.0. Third-party dependencies are not
relicensed by this repository.

The public npm packages contain positive file allowlists and do not bundle
`node_modules` or vendor Remotion source. Direct and transitive dependencies are
installed separately by npm and retain their own licenses. Exact dependency
names and versions are recorded in the relevant `package.json` and lockfile.

Remotion package metadata for the versions used by this repository identifies
the license as `SEE LICENSE IN LICENSE.md`. Review the license distributed with
each installed `remotion` and `@remotion/*` package and the
[Remotion website](https://www.remotion.dev/) before use.
