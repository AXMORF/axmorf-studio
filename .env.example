# Copy this file to .env. Relative paths resolve from the repository root.
# Absolute paths are also supported for operator-owned configuration elsewhere.
RSP_PRODUCER_CONFIG=private/producer.config.json
