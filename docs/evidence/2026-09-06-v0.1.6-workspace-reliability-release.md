# v0.1.6 Workspace reliability follow-up

## Incident and source changes

The published v0.1.5 Workspace completed a real Hermes video but failed the next Codex doctor before production.
Fixed Node.js 22.22.3 reproduced `SIGKILL` of an exiting browser process group returning `EPERM`, then an empty process
group listing and `ESRCH`. The shared bounded-process close callback threw outside its controlling Promise, so the caller
lost the child status/output and doctor crashed. Node.js 26.4.0 also reproduced the original error outside Codex.

[Apple XNU killpg1](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/kern/kern_sig.c) filters zombie group members
and can return EPERM when no signalable members remain. This supports the observed exit-race explanation; EPERM alone
must never be treated as successful cleanup. A read-only liveness observation must prove the owned group has disappeared or, after child close, contains no live member,
while real or unknown permission failures remain failures. No browser sandbox is weakened and no provider/production retry
is introduced.

The public final-check route also still used old narrative-baseline media receipts that current production does not emit.
Current Delivery inspection verified the Hermes bytes. The public route now validates current Revision, artifacts and
four-file Delivery; the explicit development proof workflow keeps its separate baseline contract.

## Verification and release

- The original cleanup-error regression failed before the fix; genuine permission denial, output retention, ownership
  registration and deadline cases are covered without weakening failures.
- A signal-zero-only check still reproduced the real browser exit race. The strict Darwin close-time process snapshot
  implementation then passed ten consecutive real 16×16 PNG renders on fixed Node.js 22.22.3 against the existing Workspace.
- Sixteen public final-check regressions passed, including missing legacy proof files, zero-write verification, checksum drift,
  stale Revision/artifact set, dirty artifacts, input changes during inspection, and six self-consistent but incorrect media specifications.
- The new public adapter also verified the actual v0.1.5 Hermes Delivery using its installed runtime manifest: artifacts,
  media, Revision, artifact set, expected DeliveryBuildId, audio channels and snapshot all passed, with no writes.

Complete repository checks passed with 776 tests and no failures or skips. Package build/typechecks, lint, documentation links,
resource/registry checks, compositions and host checks passed. Three additional doctor runs using the built CLI also passed.
Published source: `dd74681cd40dd6e670576acc2e1661d7b8d23bb3`, tag `v0.1.6`.
[macOS ARM64 gate](https://github.com/AXMORF/axmorf-studio/actions/runs/34024494885),
[npm publication](https://github.com/AXMORF/axmorf-studio/actions/runs/34024738662) and
[GitHub Release](https://github.com/AXMORF/axmorf-studio/releases/tag/v0.1.6) are complete.


Downloaded release tarballs independently match their pack receipts and official registry integrity:

| Package | Bytes | SHA-512 integrity |
| --- | ---: | --- |
| `@axmorf/studio@0.1.6` | 2495798 | `sha512-hVLedojkUhf5SU3ZhhMlYGG3onPzsgaX46HY+399BaN7jgC9enYvkDTUQpxnYalYrGaU3D9ym9cQqpXprXanRw==` |
| `create-axmorf-studio@0.1.6` | 24741 | `sha512-iy4wPCXG8sfnzoo3xAjdXXkga/n+QjvN87oXOduyK85q3YldrWmCeMxtxEGv2HrKTB2TDbpYyER9LN4D8J6jyg==` |

## Fresh official-registry installation

The v0.1.5 Workspace, including its verified Hermes video and all failure evidence, was preserved by directory rename.
A new `/Users/ai/AgentWorkspace/axmorf` was created with the official registry and
`npm exec --yes --package=create-axmorf-studio@latest -- create-axmorf-studio axmorf --yes`.
The dedicated official npm cache was reused; no local tarball or runtime override was used.

- Both official registry latest versions are 0.1.6; installed runtime version, resolved URL and lockfile integrity match.
- All 36 Remotion packages resolve exactly to 4.0.489.
- Install, bootstrap, pinned browser preparation and real-render doctor passed; the installation audit found zero vulnerabilities.
- Official-registry signature audit: 254 verified package signatures and 52 verified attestations.
- Host: macOS 15.7.1 / Darwin 24.6.0, x86_64. Node.js 22.22.3 and npm 10.9.8 were used for the controlled acceptance launch.

## Agent video acceptance

Both actual CLI hosts completed production in the fresh published v0.1.6 Workspace, without edits to installed packages.

| Host | Project | Session | Video |
| --- | --- | --- | --- |
| Codex 0.153.4 | codex-ten-minute-focus | 01a07615-4f2d-78b2-9b5c-562354ba3e2e | 967 frames, 32.233 seconds |
| Hermes 0.21.0 | hermes-compound-growth | 20260906_174917_155871 | 960 frames, 32 seconds |

Both reached `project-production-complete`, committed six Agent artifacts and delivered exact four files. Independent
checks verified checksums, 1080×1920 / 30 fps H.264, stereo AAC, both PNG covers and full EOF decoding. Selected video frames
were visually inspected. Post-production byte comparison verified all 251 runtime package files and eight creator instruction
files against published tarballs.

However, concurrent public final verification exposed another cleanup edge. The read-only observation captured a real
`SIGKILL EPERM` → signal-zero `EPERM` → process snapshot with no target group, but an unrelated row `87611 ?E` caused the
strict whole-table state parser to reject the proof. No media or Project bytes changed. This release therefore has successful
video production evidence but does not close the Mac cleanup acceptance issue; v0.1.7 addresses that remaining parser edge.
