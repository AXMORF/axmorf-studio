import { Composition } from "remotion";

import NetDriftAuthoringComposition from "./Composition";

const Root = () => (
  <Composition
    id="M7GpsNetDriftAuthoring"
    component={NetDriftAuthoringComposition}
    durationInFrames={322}
    fps={30}
    width={1920}
    height={1080}
  />
);

export default Root;
